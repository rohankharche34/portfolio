# Spotify Configuration (Backend - visitors see YOUR music)
# Get these from https://developer.spotify.com/dashboard

# Frontend build (public)
VITE_SPOTIFY_CLIENT_ID=your_client_id

# Backend (secret - add in Vercel Settings → Environment Variables)
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret
SPOTIFY_REFRESH_TOKEN=your_refresh_token

# Must match redirect URI in Spotify Dashboard
SPOTIFY_REDIRECT_URI=https://your-app.vercel.app/

# Supabase (optional - for persistent token storage)
# SUPABASE_URL=your_project_url
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key  <-- MUST use service_role, NOT anon key
#
# If using Vercel + Supabase integration, URL is auto-added as NEXT_PUBLIC_SUPABASE_URL
# You still need to manually add SUPABASE_SERVICE_ROLE_KEY in Vercel Settings
#
# SQL to run in Supabase SQL Editor:
# ALTER TABLE tokens ENABLE ROW LEVEL SECURITY;
# CREATE TABLE tokens (
#   service TEXT PRIMARY KEY,
#   refresh_token TEXT NOT NULL,
#   updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
# );